<h1 class="centre">Welcome to <%= @user.username %>'s page</h1>

<div class="row">
  <div class="col-md-4 col-md-offset-4 centre">
    <%= gravatar_for @user, size: 150 %>
  </div>
</div>
<h4 class="centre"><%= @user.username %>'s articles</h4>

<%= render 'articles/article', obj: @user.articles %>
